import { UserList } from '@/features/users/components/UserList';

export const metadata = {
  title: 'User Management | Infano Admin',
};

export default function UsersPage() {
  return <UserList />;
}
